# Required: Key used for encrypting sensitive configuration values
# Generate a secure key and keep it safe - if lost, encrypted values cannot be recovered
CONFIG_ENCRYPTION_KEY=your-secure-key-here

# Example of how to generate a secure key:
# On Unix/Linux: openssl rand -base64 32
# On Windows PowerShell: [Convert]::ToBase64String([Security.Cryptography.RandomNumberGenerator]::GetBytes(32))
